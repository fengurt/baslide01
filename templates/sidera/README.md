# Sidera 8 layouts

Open `layouts.html`. Canvas 1440×810, scaled to the viewport. Keyboard ← →.

Clone the `<section class="slide layout-…">` that matches the page. Tokens and class names are locked; see `skills/sidera-html-slides/SKILL.md`.
